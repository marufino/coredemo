<div class="col-xs-12 shadow user-profile" style="margin-bottom: 30px">
    <div class="col-xs-3 user-profile">
      <%= image_tag("profile_thumb.png", size: "145x125", :alt => "rss feed", :style => "padding-left: 20px" ) %>
    </div>
    <div class="col-xs-3 user-profile">
      <b><%= user.first_name %> <%= user.last_name %></b><br>
      <small><%= user.title %></small><br>
      <small><%= @last_assignment.date %></small><br>
      <% if !@second_to_last_assignment.nil? %>
      <small>Last evaluation: <%= @second_to_last_assignment.date %></small><br>
      <% end %>
      <small>Evaluation by:</small>
        <% @observers.each do |o| %>
          <small><%= o.user.first_name %> <%= o.user.last_name %></small><br>
        <% end %>
      <small>Results:</small><small class="text-success">PASSED</small><br>
    </div>
    <div class = "col-xs-4">
      Project: <%= Project.find(@score.assignment.project_id).name %><br>
      Date: <%= @score.assignment.date %><br>
      Observer: <%= current_user.first_name %> <%= current_user.last_name %>
    </div>
    <div class = "col-xs-2">
      <h1 style="color: greenyellow"><strong>84</strong></h1>
      <h4>CORE Score</h4>
    </div>
</div>

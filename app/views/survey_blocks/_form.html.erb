<%= form_for(@survey_block) do |f| %>
  <% if @survey_block.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_block.errors.count, "error") %> prohibited this survey_block from being saved:</h2>

      <ul>
      <% @survey_block.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>
  <div class="field">
    <%= f.label :weight %><br>
    <%= f.number_field :weight %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@test_score) do |f| %>
  <% if @test_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_score.errors.count, "error") %> prohibited this test_score from being saved:</h2>

      <ul>
      <% @test_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

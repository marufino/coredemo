<%= form_for(@score) do |f| %>
  <% if @score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@score.errors.count, "error") %> prohibited this score from being saved:</h2>

      <ul>
      <% @score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <p>Scores</p>

    <% @blocks_ratings.each do |block, ratings| %>
        <%= block.category %>
        <% block.questions.each do |b| %>
            <%= b.category %>
            <% end %>
        <% end %>

    <%= f.fields_for :ratings do |ratings_form| %>
          <td>
            <%= ratings_form.number_field :value %>
          </td>
    <% end %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
